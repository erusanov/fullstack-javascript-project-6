extends ../layouts/application.pug

block content
  .container.mt-3
    h1= t('views.taskStatus.editStatus')

    form(action=app.reverse('taskStatusesUpdate', { id: taskStatus.id }) method='post')
      input(type="hidden" name="_method" value="patch")
      .mb-3
        label.form-label(for='data_name')= t('views.taskStatus.name')
        input.form-control(type='text' id='data_name' name='data[name]' value=taskStatus.name)
        if errors && errors.name
          .text-danger= errors.name.message
      button.btn.btn-primary(type='submit')= t('views.taskStatus.edit')
